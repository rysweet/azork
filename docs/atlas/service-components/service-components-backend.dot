digraph service_components_backend { graph [rankdir=TB, label="service-components backend", labelloc=t]; node [shape=box, style="rounded"];
  backend_mod [label="backend/mod.rs"]; trait_backend [label="Backend trait"]; select [label="select and is_recognized"]; mock_env [label="mock_backend_from_env"]; mock [label="backend/mock.rs MockBackend"]; fixed [label="MockWorldSpec Fixed"]; sized [label="MockWorldSpec Sized"]; mock_gen [label="backend/mock_gen.rs"]; params [label="MockSizeParams parse from_env"]; presets [label="MockSizePreset small medium large huge"]; rng [label="SplitMix64 Rng"]; gen_world [label="generate_world"]; fake_runner [label="fake_runner for crawl"]; az [label="backend/az.rs AzBackend"]; az_runner [label="AzRunner trait"]; process_runner [label="ProcessAzRunner"]; world [label="world::World Room Resource"]; parser [label="parser::Direction"]; concurrency [label="dungeon::concurrency backoff"]; secrets [label="secrets::scrub"];
  backend_mod -> trait_backend; backend_mod -> select; select -> az; select -> mock_env; mock_env -> mock; mock_env -> mock_gen; mock -> fixed; mock -> sized; mock -> world; mock -> parser; sized -> params; mock_gen -> params; mock_gen -> presets; mock_gen -> rng; mock_gen -> gen_world; mock_gen -> fake_runner; gen_world -> world; fake_runner -> az_runner; az -> az_runner; az -> process_runner; az -> world; az -> concurrency; az -> secrets;
}
